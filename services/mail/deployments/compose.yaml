services:
  mail:
    build:
      context: ..
      dockerfile: deployments/Dockerfile
    restart: unless-stopped
    env_file: mail.env
    read_only: true
    cap_drop: [ALL]
    security_opt: [no-new-privileges:true]
    mem_limit: 128m
    cpus: 0.25
